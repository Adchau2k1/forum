// uno.config.ts
import {
  defineConfig,
} from 'unocss'

export default defineConfig({
  theme: {
    colors: {
      primary: '#22e1b9',
      textColor: '#333',
    },
    boxShadow: {
      pretty: 'rgba(0, 0, 0, 0.16) 0px 3px 6px, rgba(0, 0, 0, 0.01) 0px 3px 6px',
    }
  }
})

